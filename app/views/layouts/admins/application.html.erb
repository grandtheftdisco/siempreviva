<!DOCTYPE html>
<html>
  <%= render partial: "layouts/public/head" %>

  <body>
    <header>
      <nav class="flex justify-between p-5">
        <p>[logo]</p>
        <%= link_to 'Admin Dashboard', admins_orders_path %>
        <ul class="inline-flex gap-5">
          <li>
            <%= button_to 'Log Out', 
                  session_path, 
                  method: :delete %>
          </li>
        </ul>
      </nav>
    </header>
    <main>
      <%= render partial: "layouts/public/flash_alert" %>
      <%= yield %>
    </main>
  </body>
</html>