<!DOCTYPE html>
<html>
  <%= render partial: "layouts/public/head" %>

  <body>
    <header>
      <nav class="flex justify-between p-5">
        <ul class="inline-flex gap-5">
          <li>View All Orders</li>
          <li>
            <%= button_to 'Log Out', 
                  session_path, 
                  method: :delete %>
          </li>
        </ul>
      </nav>
    </header>
    <main>
      <p>layouts/public/_head.html.erb</p>
      <%= render partial: "layouts/public/flash_alert" %>
      <%= yield %>
    </main>
  </body>
</html>