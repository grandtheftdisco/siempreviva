<%# NOTE: Temporary styling - this entire partial will be replaced when product-views branch merges into main %>
<%# No need to refactor inline Tailwind or update component classes beyond form label (Session 4A requirement) %>
<div class="bg-green-200 p-5">
  <p><%= link_to "#{product.name.titleize}", product_path(product.id) %></p>
  <% product.images.each do |i| %>
    <%= image_tag(i, width: 200) %>
  <% end %>
  <p>Price: <%= to_dollars(product.default_price.unit_amount) %></p>

  <%= form_for :cart_item, url: cart_items_path, method: :post do |f| %>
    <%= hidden_field_tag 'cart_item[stripe_product_id]', product.id %>
    <%= f.label :quantity, class: 'label-text' %>:
    <%= f.number_field :quantity,
      class:'bg-yellow-200',
      value: 1,
      min: 1 %>
    <%= f.submit "Add to Bag", class: 'btn-green' %>
  <% end %>
</div>